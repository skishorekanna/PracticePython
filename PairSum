def pairsum():
    x = [10,2,9,3,7,8,13,-1]
    x =  sorted(x)
    l = 0
    h = len(x)-1
    n = 12
    while(l<h):
        if((x[l]+x[h])==n):
            print x[l],x[h]
            l+=1
            h-=1
        elif((x[l]+x[h])<n):
            l+=1
        else:
            h-=1
